export interface MyLocaleOptions {
    timeLocaleOptions: Array<any>;
    customRangeTitle: string;
    quickRangesTitle: string;
    fromText: string;
    toText: string;
    refreshValueOptions: Array<any>;
    refreshingEvery: string;
    apply: string;
    zoomTooltipMsg: string;
    refreshTooltipMsg: string;
}
